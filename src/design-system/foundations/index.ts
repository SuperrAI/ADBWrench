/**
 * Design System Foundations
 * 
 * This file exports all foundation elements of the design system.
 */

import colors from './colors'
import { typography, textStyles } from './typography'
import { spacing, radii, shadows, zIndices } from './spacing'
import { durations, easings, keyframes, animations } from './animations'
import { iconSizes, ICONS_PATH } from './icons'

export {
  colors,
  typography,
  textStyles,
  spacing,
  radii,
  shadows,
  zIndices,
  durations,
  easings,
  keyframes,
  animations,
  iconSizes,
  ICONS_PATH,
}

const foundationsExport = {
  colors,
  typography,
  textStyles,
  spacing,
  radii,
  shadows,
  zIndices,
  durations,
  easings,
  keyframes,
  animations,
  iconSizes,
  ICONS_PATH,
};

export default foundationsExport; 